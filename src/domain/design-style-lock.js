export function buildDesignReferenceConsistencyInstructions(reference) {
  if (!reference) return [];
  const instructions = [
    "STYLE LOCK ПО РЕФЕРЕНСУ: все генерации с этим дизайн-референсом должны ощущаться как одна серия, сделанная одним дизайнером.",
    "Разрешено менять только композиционный формат под тему: список, таблица, сравнение, схема, карточки или таймлайн. Нельзя каждый раз пересобирать новый визуальный мир.",
    "Стабилизировать одни и те же постоянные признаки: семейство фона, температуру палитры, стиль света, типографическую иерархию, форму карточек, характер иконок, материалы 3D-объектов, толщину обводок и глубину теней.",
    "Если передан один сильный референс, визуальное сходство по стилю должно быть высоким: примерно 80-90% той же визуальной ДНК без копирования чужого текста и сюжета.",
    "Не прыгать между разными школами дизайна: не делать один раз flat SaaS, потом glossy medical, потом editorial poster, если референс задает один устойчивый язык."
  ];
  if (reference.takeaways) instructions.push(`СТАБИЛЬНЫЕ ПРИЗНАКИ ИЗ РЕФЕРЕНСА: ${reference.takeaways}.`);
  if (reference.palette) instructions.push(`ФИКСАЦИЯ ПАЛИТРЫ: держать ту же цветовую семью и контрастность, что в референсе: ${reference.palette}.`);
  if (reference.fontStyle || reference.headlineStyle) instructions.push(`ФИКСАЦИЯ ТИПОГРАФИКИ: не менять характер шрифтов между генерациями. Использовать один и тот же типографический почерк: ${reference.fontStyle || reference.headlineStyle}.`);
  if (reference.textDensity) instructions.push(`ФИКСАЦИЯ ПЛОТНОСТИ: держать ту же плотность и ритм информации, что в референсе: ${reference.textDensity}.`);
  return instructions;
}
